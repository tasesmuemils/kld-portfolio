---
const { video, title, description } = Astro.props;
---

<div
  class="small-slider-item absolute w-[141px] h-[250px] inline-block top-[40%] z-[1]"
>
  <div class="video-content relative w-full h-full">
    <video
      autoplay
      loop
      muted
      class="absolute inset-0 w-full h-full object-cover rounded-2xl shadow-[0_2px_15px_-3px_rgba(0,0,0,0.07),0_10px_20px_-2px_rgba(0,0,0,0.04)] dark:bg-[hsla(0,0%,5%,0)] dark:shadow-black/20"
    >
      <source src={video} type="video/mp4" />
    </video>
    <!-- <div
      class="small-slider-item-content absolute top-1/4 left-10 w-[80%] translate-x-0 -translate-y-1/2 text-left text-white hidden"
    >
      <h2 class="text-2xl font-bold mb-4">{title}</h2>
      <p class="mb-4">
        {description}
      </p>
      <a
        href="#"
        class="inline-block bg-blue-500 text-white py-2 px-4 rounded hover:bg-blue-600"
        >Learn More</a
      >
    </div> -->
  </div>
</div>
